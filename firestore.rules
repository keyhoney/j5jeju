rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 공개 일정 (기본 컬렉션명: jeju_schedule_public). 다른 이름을 쓰면 동일 규칙을 복제해 경로를 추가하세요.
    match /{collection}/{docId} {
      allow read, write: if collection == 'jeju_schedule_public';
    }
  }
}
